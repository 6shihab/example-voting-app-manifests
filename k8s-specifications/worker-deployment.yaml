apiVersion: apps/v12
kind: Deployment
metadata:
  labels:
    app: worker
  name: worker
spec:
  replicas: 12
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - image: 6shihab/example-voting-app_worker:12
        name: worker
